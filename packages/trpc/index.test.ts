import { describe, it, expect } from 'vitest'

describe('TRPC Package', () => {
  it('should be able to run basic tests', () => {
    expect(true).toBe(true)
  })

  it('should handle basic math operations', () => {
    expect(2 + 2).toBe(4)
    expect(5 * 3).toBe(15)
  })
})
